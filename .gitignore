/auths/
/state.json
/state.json.tmp
/config.yaml
/tokens.json
/logs/
/bin/
*.log
*.bak
.env
.env.*

# Admin SPA build output & deps
/internal/admin/web/node_modules/
/internal/admin/web/dist/*
!/internal/admin/web/dist/.gitkeep
.claude/

# crack/ — local-only redaction map with real captured secrets
crack/scripts/redaction_map.json

# Local-only Go workspace, used to iterate on the sibling cc-core checkout.
# A `replace` in go.mod would do the same thing and has broken CI here before.
go.work
go.work.sum
